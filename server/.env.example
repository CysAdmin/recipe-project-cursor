PORT=3001
JWT_SECRET=your-super-secret-jwt-key-change-in-production
NODE_ENV=development
DATABASE_PATH=./data/recipes.db
# Email verification (Resend)
RESEND_API_KEY=re_xxxx
# Frontend base URL (no trailing slash). Used for CORS and for verification/reset links in emails. Production: https://deine-app.vercel.app
FRONTEND_URL=http://localhost:5173
# Optional: custom from address (default: SimplyKeepIt <noreply@simplykeepit.com>)
# EMAIL_FROM=SimplyKeepIt <noreply@simplykeepit.com>
# Optional: Resend template ID (default: 58d1be20-87e7-459d-90ef-0c124896f071)
# RESEND_VERIFICATION_TEMPLATE_ID=58d1be20-87e7-459d-90ef-0c124896f071
# Password reset: Resend template ID for forgot-password emails (create template in Resend, set variables PASS_RESET_TOKEN, USER_NAME)
# RESEND_PASSWORD_RESET_TEMPLATE_ID=

# External recipe search + URL import: User-Agent for HTTP requests (use a normal browser UA so sites like Tasty don't block)
# SEARCH_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36

# Optional: nur fürs Skript "npm run create-admin" – ersten Admin anlegen (Passwort besser per Kommandozeile übergeben)
# ADMIN_EMAIL=admin@beispiel.de
# ADMIN_PASSWORD=DeinSicheresPasswort