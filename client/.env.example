# API base URL (without trailing slash). Leave empty for same-origin (local dev with proxy or monorepo).
# Production (Vercel): set in Vercel project → Settings → Environment Variables, e.g. https://api.yourdomain.com
VITE_API_URL=

# Absolute App-URL for canonical/OG tags (without trailing slash).
# Local development: http://localhost:5173
# Production: https://simplykeepit.com
VITE_APP_URL=https://simplykeepit.com
